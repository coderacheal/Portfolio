const email = document.querySelectorAll('.input-individuals');
console.log(email)
const form = document.querySelector('.form-container')

form.addEventListener('submit', (e) => {
    let container = [];

    let enteredEmail = document.email.inner

    if (email.value !== email.value.lower )
    // if (email.)
    e.preventDefault()
})